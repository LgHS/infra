{% for cidr in cloudflare_proxies_ipv4.content.splitlines()
             + cloudflare_proxies_ipv6.content.splitlines() %}
    set_real_ip_from {{ cidr }};
{% endfor %}

real_ip_header X-Forwarded-For;
